import React from 'react'

const About = () => {
  return (
    <div>
        adfasdfasdf
        
        About
        Create React App doesn't include page routing.

dfasdfadfasdfasdfasd
        Create React App doesn't include page routing.

        Create React App doesn't include page routing.

        Create React App doesn't include page routing.

        Create React App doesn't include page routing.

        Create React App doesn't include page routing.

<div className="mt-5 p-4 bg-dark text-white text-center">
  <p>Footer</p>
</div>  
    </div>
  )
}

export default About